/**
 * BOE custom function — STUB (module 5, build last).
 *
 * =BOE("AAPL", "Revenue", "2023", "Q4")
 *
 * Will call GET /api/v1/financials?ticker=&year=&quarter= with an
 * Authorization: Bearer header once the add-in project is bootstrapped.
 */
export {};
